# API.md — <Project>

Tree root. <What the system exposes outward: commands, endpoints, public types, result
files, in one or two sentences.>

## How the system is used ⏳

<The scenario from outside, without declarations: who calls, what they pass, what they
get, where the results appear. Reads as a path through the nodes, with links to their
`API.md`.>

## Entry points ⏳

```<language>
<the system's entry points: CLI commands, endpoints, public library types>
```

## Children

- [<Node>](./<path>/API.md) — <one line: what the node exposes>.

<!-- Internal nodes that nobody uses from outside need not be listed here: the section
     describes the surface of the system, not the table of contents of the directory. -->

## Test nodes

- [<Tests node>](./tests/<path>/API.md) — <what this node proves>.
