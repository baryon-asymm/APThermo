<Project Sdk="Microsoft.NET.Sdk">

  <!-- Node src/Performance: the rocket problem of one case with an infinite-area chamber (RP-1311 chapter 6). Kernel-compatible. -->
  <ItemGroup>
    <ProjectReference Include="..\Equilibrium\APThermo.Equilibrium.csproj" />
    <ProjectReference Include="..\Thermo\APThermo.Thermo.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ILGPU" />
  </ItemGroup>

  <!-- The tree contract of src/Performance (root BOOT.md, Delivery: Tree contracts): the src nodes that
       declare Performance in their ## Dependencies, and their mirroring test nodes plus Benchmarks. -->
  <ItemGroup>
    <InternalsVisibleTo Include="APThermo.Performance.Tests" />
    <InternalsVisibleTo Include="APThermo.Execution" />
    <InternalsVisibleTo Include="APThermo.Execution.Tests" />
    <InternalsVisibleTo Include="APThermo.Problems" />
    <InternalsVisibleTo Include="APThermo.Benchmarks" />
  </ItemGroup>

</Project>
