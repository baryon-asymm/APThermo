<Project Sdk="Microsoft.NET.Sdk">

  <!-- Node tests/Equilibrium.Tests: the readiness criterion of src/Equilibrium. CPU accelerator only. -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\Equilibrium\APThermo.Equilibrium.csproj" />
    <ProjectReference Include="..\..\src\Thermo\APThermo.Thermo.csproj" />
    <ProjectReference Include="..\..\src\Data\APThermo.Data.csproj" />
    <ProjectReference Include="..\Fixtures\APThermo.Fixtures.csproj" />
    <ProjectReference Include="..\Harness\APThermo.Harness.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ILGPU" />
  </ItemGroup>

  <!-- ILGPU 1.5.3 needs only InternalsVisibleTo("ILGPURuntime") on this assembly to reach the internal
       BatchViews (KernelEqualityTests.cs) as a kernel parameter type; ILGPU emits its kernel wrappers into a
       dynamic assembly of that name (root BOOT.md, Delivery: Tree contracts). -->
  <ItemGroup>
    <InternalsVisibleTo Include="ILGPURuntime" />
  </ItemGroup>

</Project>
