<Project Sdk="Microsoft.NET.Sdk">

  <!-- Node tests/Harness: the scaffolding the test nodes share. A plain library, not a test project. -->
  <PropertyGroup>
    <IsTestProject>false</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Data\APThermo.Data.csproj" />
    <ProjectReference Include="..\Fixtures\APThermo.Fixtures.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ILGPU" />
    <!--
      TheoryData<> only (FixtureFamilies.Of): this stays a plain library (IsTestProject false, above), not a test
      project, so tests/Directory.Build.props does not add the full xunit/Test.Sdk/runner set it gives real test
      projects.
    -->
    <PackageReference Include="xunit" />
  </ItemGroup>

</Project>
